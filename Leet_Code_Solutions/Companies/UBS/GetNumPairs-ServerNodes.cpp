#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> server_from = {1, 1, 2};
    vector<int> server_to = {2, 3, 4};

    vector<int> server_weight = {2, 5, 3};

    getNumPairs(4, server_from, server_to, server_weight, signal)
}